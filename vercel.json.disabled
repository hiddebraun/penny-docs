{
  "buildCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && source $HOME/.cargo/env && uv sync && uv run mkdocs build",
  "outputDirectory": "site",
  "installCommand": "echo 'Using buildCommand for installation'",
  "framework": null
}
